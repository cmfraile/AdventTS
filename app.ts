console.clear();

const main = (altura:number) => {
    let cadena = "";
    let hojas = 1;
    let ancho = (altura * 2) - 1;
    for(let i = 1 ; i <= altura ; i++){
        let piso = "";
        for(let y = 1 ; y <= ancho ; y++){
            if(y <)
        }
    }
}

main(5);