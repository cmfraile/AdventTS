console.clear();

const carta = "bici coche (balón) bici coche peluche";

const main = () => {
    const analisis = {
        inicio : carta.indexOf('('),
        fin : carta.indexOf(')'),
        grinched : carta.match()
    }
}

main();